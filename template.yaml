# This AWS SAM template has been generated from your function's configuration. If
# your function has one or more triggers, note that the AWS resources associated
# with these triggers aren't fully specified in this template and include
# placeholder values. Open this template in AWS Infrastructure Composer or your
# favorite IDE and modify it to specify a serverless application with other AWS
# resources.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model template describing your function.
Resources:
  urlNotalambda:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: registra-nota-usuario
      MemorySize: 512
      Timeout: 30
      Handler: poupacompra.registranotausuario.App::handleRequest
      Runtime: java21
      Architectures:
        - x86_64
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          SQS_QUEUE_URL: https://sqs.eu-central-1.amazonaws.com/203689707723/UrlNFCe
          AWS_REGION: eu-central-1
      EventInvokeConfig:
        MaximumEventAgeInSeconds: 21600
        MaximumRetryAttempts: 2
        DestinationConfig:
          OnFailure:
            Destination: !Ref SQSQueueDestinationFailure1
            Type: SQS
      PackageType: Zip
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - sqs:SendMessage
              Resource: arn:aws:sqs:eu-central-1:203689707723:UrlNFCe
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
              Resource: arn:aws:logs:eu-central-1:203689707723:*
            - Effect: Allow
              Action:
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource:
                - arn:aws:logs:eu-central-1:203689707723:log-group:/aws/lambda/urlNota_lambda:*
      RecursiveLoop: Terminate
      SnapStart:
        ApplyOn: PublishedVersions
      Events:
        Api1:
          Type: Api
          Properties:
            Path: /registrar-nota-usuario
            Method: POST
      RuntimeManagementConfig:
        UpdateRuntimeOn: Auto
  SQSQueueDestinationFailure1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: SQSQueueDestinationFailure1
      SqsManagedSseEnabled: true