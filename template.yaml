# This AWS SAM template has been generated from your function's configuration. If
# your function has one or more triggers, note that the AWS resources associated
# with these triggers aren't fully specified in this template and include
# placeholder values. Open this template in AWS Infrastructure Composer or your
# favorite IDE and modify it to specify a serverless application with other AWS
# resources.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model template describing your function.
Resources:
  registraNfce:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: registra-nota-usuario
      MemorySize: 512
      Timeout: 30
      Handler: poupacompra.registranotausuario.App::handleRequest
      Runtime: java21
      Architectures:
        - x86_64
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          SQS_QUEUE_URL: urlLambda
          SQSQUEUEDESTINATIONFAILURE1_QUEUE_NAME: !GetAtt SQSQueueDestinationFailure1.QueueName
          SQSQUEUEDESTINATIONFAILURE1_QUEUE_ARN: !GetAtt SQSQueueDestinationFailure1.Arn
          SQSQUEUEDESTINATIONFAILURE1_QUEUE_URL: !Ref SQSQueueDestinationFailure1
      EventInvokeConfig:
        MaximumEventAgeInSeconds: 21600
        MaximumRetryAttempts: 2
        DestinationConfig:
          OnFailure:
            Destination: !Ref SQSQueueDestinationFailure1
            Type: SQS
      PackageType: Zip
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - sqs:SendMessage
              Resource: !Sub arn:aws:sqs:eu-central-1:000000000000:registra-nfce
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
              Resource: !Sub arn:aws:logs:eu-central-1:000000000000:*
            - Effect: Allow
              Action:
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource:
                - !Sub arn:aws:logs:eu-central-1:000000000000:log-group:/aws/lambda/registraNfce:*
        - SQSSendMessagePolicy:
            QueueName: !GetAtt SQSQueueDestinationFailure1.QueueName
      RecursiveLoop: Terminate
      SnapStart:
        ApplyOn: PublishedVersions
      Events:
        Api1:
          Type: Api
          Properties:
            Path: /registrar-nota-usuario
            Method: POST
      RuntimeManagementConfig:
        UpdateRuntimeOn: Auto
  SQSQueueDestinationFailure1:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: registra-nfce
      SqsManagedSseEnabled: true